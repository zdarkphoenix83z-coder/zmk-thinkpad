name: ZMK Build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: ZMK Build
        uses: zmkfirmware/zmk/.github/actions/build@main
        with:
          board: nice_nano_v2
